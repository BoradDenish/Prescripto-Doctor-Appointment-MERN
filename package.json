{
  "name": "fullstack-app",
  "private": true,
  "scripts": {
    "install-all": "npm --prefix admin install && npm --prefix backend install && npm --prefix frontend install",
    "dev": "concurrently \"npm run server --prefix backend\" \"npm run dev --prefix frontend\" \"npm run dev --prefix admin\""
  },
  "dependencies": {
    "@heroicons/react": "^2.0.0",
    "@react-spring/web": "^9.7.5",
    "framer-motion": "^11.15.0",
    "react-icons": "^5.4.0",
    "concurrently": "^8.2.0"
  }
}
